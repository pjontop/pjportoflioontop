<section class="showreel" aria-label="Showreel preview">
	<header class="showreel__header">
		<img src="/status-dot.svg" alt="" width="11" height="11" />
		<p>SHOWREEL</p>
	</header>

	<div class="showreel__screen" aria-hidden="true">
		<div class="showreel__mark">
			<span></span>
			<span></span>
		</div>
	</div>
</section>

<style>
	.showreel {
		position: absolute;
		top: 24.875rem;
		left: 50%;
		z-index: 2;
		width: 16.8125rem;
		height: 13.1875rem;
		padding: 0.5625rem 0.6875rem 0.75rem;
		border-radius: 0.375rem;
		background: var(--color-old-navy);
		color: var(--color-porridge);
		transform: translateX(-50%);
	}

	.showreel__header {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
	}

	.showreel__header img {
		flex: 0 0 0.6875rem;
		width: 0.6875rem;
		height: 0.6875rem;
	}

	.showreel__header p {
		margin: 0;
		font-family: "Manrope", system-ui, sans-serif;
		font-size: 0.6875rem;
		font-weight: 600;
		line-height: 1;
		letter-spacing: -0.0172rem;
	}

	.showreel__screen {
		position: absolute;
		right: 0.6875rem;
		bottom: 0.75rem;
		left: 0.6875rem;
		height: 7.1875rem;
		overflow: hidden;
		border-radius: 0.25rem;
		background: var(--color-porridge);
	}

	.showreel__mark {
		position: absolute;
		top: 50%;
		left: 50%;
		width: 1.5rem;
		height: 1.5rem;
		opacity: 0;
		transform: translate(-50%, -50%) rotate(45deg);
		transition: opacity 180ms ease;
	}

	.showreel__mark span {
		position: absolute;
		top: 50%;
		left: 50%;
		width: 100%;
		height: 1px;
		background: var(--color-blueberry);
		transform: translate(-50%, -50%);
	}

	.showreel__mark span:last-child {
		transform: translate(-50%, -50%) rotate(90deg);
	}

	.showreel:hover .showreel__mark {
		opacity: 1;
	}

	@media (max-width: 47.99rem) {
		.showreel {
			top: 20.75rem;
			width: min(16.8125rem, calc(100vw - 3rem));
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.showreel__mark {
			transition: none;
		}
	}
</style>
